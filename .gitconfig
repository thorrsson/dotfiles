[user]
	name = Tim Hunter
	email = thorrsson@github.com
[alias]
	s = status
	rv = remote -v
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[url "git@github.com:"]
	insteadOf = https://github.com/